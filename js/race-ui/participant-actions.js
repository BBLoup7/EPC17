/**
 * Participant Actions sub-module for RaceUI
 * Target methods: showParticipantContextMenu, handleParticipantAction,
 *   handleSwapParticipant, handleSwapSameHeat, handleMoveToEmpty, handleMoveToOtherHeat,
 *   handleFalseStart, handleDisqualify, handleRetroactiveDisqualify
 * @module race-ui/participant-actions
 */
// Stub — methods remain in race-ui.js during incremental migration.
