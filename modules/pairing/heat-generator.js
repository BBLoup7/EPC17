/**
 * Heat Generator sub-module for PairingEngine
 * Core heat generation algorithms (exhaustive, greedy, fallback).
 * 
 * Target methods to migrate from pairing-engine.js:
 *   - generateHeats()
 *   - generateExhaustiveHeats()
 *   - generateGreedyHeats()
 *   - generateFallbackHeats()
 *   - scoreCandidate()
 *   - getNextRaceNumber()
 *   - setNextRaceNumber()
 * 
 * @module pairing/heat-generator
 */
// Stub — methods remain in pairing-engine.js during incremental migration.
