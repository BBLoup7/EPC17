"""
Auth Blueprint — Login, Logout, Check-Auth routes
Target routes to migrate from server.py:
    POST /api/auth/login
    POST /api/auth/logout  
    GET  /api/auth/check
"""
# from flask import Blueprint
# auth_bp = Blueprint('auth', __name__)
# Routes will be migrated here incrementally from server.py
