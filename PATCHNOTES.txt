# EPC17 Patch Notes (Detailed — documentation & trace)

All changes and LLM edits must add an entry below. Version lives in version.json; bump with:
  python scripts/version_bump.py        (patch, default)
  python scripts/version_bump.py minor
  python scripts/version_bump.py major

Entry format (use for every new change):
  ## [X.Y.Z] - YYYY-MM-DD - type
  Type: patch | minor | major
  ### Summary
  One-line for CHANGELOG/publication.
  ### Details
  Full description, file lists, technical notes.
  ### Files
  - path/to/file
  ### Meta
  Source: LLM | Session: (optional)
---

---

---

## [2.11.3] - 2026-08-04 - patch

### Summary
Series detail form uses dark filled controls instead of browser-default white inputs.

### Details
- Added `form-control` to series basics/season/class fields.
- Series page CSS now styles inputs/selects/textareas with dark surfaces, focus rings, and date-picker theming; toned-down remove buttons.

### Files
- styles/pages/series.css, modules/series-ui.js, modules/series-form.js
- PATCHNOTES.txt, version.json, CHANGELOG.md

### Meta
Source: LLM | Session: Series form control styling

---

## [2.11.2] - 2026-08-04 - patch

### Summary
Fix Series page load error: use DataManager.loadFromStorage instead of nonexistent loadAllData.

### Details
- series.html init called `loadAllData`, which is not a DataManager method; switched to `loadFromStorage(['series', 'events'])` to match other pages.

### Files
- series.html
- PATCHNOTES.txt, version.json, CHANGELOG.md

### Meta
Source: LLM | Session: Series page load fix

---

## [2.11.1] - 2026-08-04 - patch

### Summary
Print bill no longer uses a popup window — prints via a hidden iframe so browsers don’t block it.

### Details
- `RegistrationBill.openBill` writes into a same-page iframe and calls `print()` there instead of `window.open` (which returned null with `noopener` / popup blockers).

### Files
- modules/registration-bill.js
- PATCHNOTES.txt, version.json, CHANGELOG.md

### Meta
Source: LLM | Session: Print bill popup fix

## [2.11.0] - 2026-08-04 - minor

### Summary
Series page redesigned as a list + detail workspace with short names, archive/duplicate, and practical event integration.

### Details
- Rebuilt Series UI (Users-style list + persistent detail editor) with search, status filter (active/archived), and sorting.
- Extended series schema with `shortName` and `defaultSeasonId`; status simplified to active | archived (legacy completed/cancelled map to archived).
- Soft-archive is the primary retirement path; hard delete blocked when events are linked.
- Duplicate copies seasons/classes with new IDs and clears events/standings.
- Events page: series filter dropdown, short-name display on cards/details, default season preselected when creating events.
- Removed legacy inline configure UI and stubbed series.css replaced with real page styles.

### Files
- utils/db_manager.py, server.py
- modules/series.js, modules/series-data.js, modules/series-logic.js, modules/series-ui.js, modules/series-form.js
- series.html, styles/pages/series.css
- events.html, modules/event-ui.js
- PATCHNOTES.txt, version.json, CHANGELOG.md

### Meta
Source: LLM | Session: Series page redesign

---

---

## [2.10.2] - 2026-08-04 - patch

### Summary
Registration Payment panel expanded into desk helpers: mark paid, method/note, printable bill, copy fee summary.

### Details
- Payment column adds status (Pending/Paid), method, optional note — saved with create/update (`paymentMethod`, `paymentNote`, `paidAt` columns).
- Mark paid, Print bill (formal printable receipt window), and copy fee summary actions.
- New `modules/registration-bill.js` builds a print-friendly registration bill from the live form + event.

### Files
- modules/registration-bill.js, modules/existing-driver-registration.js
- components/registration-form.html, registration.html
- utils/db_manager.py, styles/pages/registration.css
- PATCHNOTES.txt, version.json, CHANGELOG.md

### Meta
Source: LLM | Session: Registration payment helpers

## [2.10.1] - 2026-08-04 - patch

### Summary
Fix existing-driver selection mode and fit registration on one screen with a 4-column layout.

### Details
- Selecting a search match now locks **existing** mode, hides the match panel, and no longer re-searches with a concatenated mega-query.
- Live search uses the field being typed (single primary term) instead of joining all identity fields.
- Registration form reorganized into 4 compact columns: Driver | Contact | Classes | Sponsors/Payment/Actions.
- Compact CSS tightened so submit actions stay visible without page scroll on wide screens.

### Files
- modules/driver-search.js, modules/existing-driver-registration.js
- registration.html, components/registration-form.html
- styles.css, styles/pages/registration.css
- PATCHNOTES.txt, version.json, CHANGELOG.md

### Meta
Source: LLM | Session: Unified Registration follow-up

## [2.10.0] - 2026-08-04 - minor

### Summary
Unified driver registration: one form finds existing drivers as you type (including license number), populates matches, and blocks accidental duplicates.

### Details
- Merged New Driver and Existing Driver into `registration.html` with live multi-field search, mode badges (new vs existing), duplicate warnings, and a session “recently registered” strip.
- Added optional `licenseNumber` on participants; SQL search now covers name, nickname, racing number, license, team, contact JSON, and `_searchText` (rebuilt on write).
- New modules: `driver-search.js` (typeahead), `duplicate-detection.js` (modern scoring incl. license/racing/DOB fix).
- `existing-drivers.html` redirects to unified registration (query params preserved); removed from sidebar and home.
- Driver edit form includes license number for parity.

### Files
- utils/db_manager.py, utils/data-manager.js
- modules/driver-search.js, modules/duplicate-detection.js, modules/existing-driver-registration.js, modules/driver-edit-form.js
- components/registration-form.html, registration.html, styles/pages/registration.css
- existing-drivers.html, js/sidebar-nav.js, index.html, docs/GUIDE.md
- PATCHNOTES.txt, version.json, CHANGELOG.md

### Meta
Source: LLM | Session: Unified Driver Registration

## [2.9.0] - 2026-08-04 - minor

### Summary
Full RBAC redesign: modern login flow with return URL and Remember Me, roles as first-class entities, and a polished Users/Roles/Settings admin console.

### Details
- Auth: Bearer restore on `/api/auth/me`, login `?next=` return path, Remember Me (localStorage vs sessionStorage + cookie max_age), logout clears server session via Bearer, public home/login without forced redirect.
- Login UI: Carbon Velocity redesign with show/hide password, loading state, clear errors (`styles/pages/login.css`).
- RBAC: `roles` table, seeded system roles, user `roleId` + `status` (active/disabled), migration from legacy per-user permissions, `/api/roles` CRUD + clone, users API returns sanitized role-resolved data (no passwords).
- Users page: tabs for Users / Roles / Settings; search, sort, status filter, avatars, modals, confirmation dialogs (`modules/users-admin.js`, `styles/pages/users.css`).
- Aligned client/server page gates including `animator.html`; updated GUIDE.md RBAC docs.

### Files
- utils/auth.js, login.html, styles/pages/login.css
- utils/db_manager.py, server.py
- users.html, modules/users-admin.js, styles/pages/users.css
- docs/GUIDE.md, PATCHNOTES.txt, version.json, CHANGELOG.md

### Meta
Source: LLM | Session: Auth RBAC Redesign

---

## [2.8.4] - 2026-06-09 - patch

### Summary
Repository cleanup: remove tests, DB backups, dead migration scripts, and outdated docs; align README and rules with Flask + HTML stack.

### Details
- Deleted entire `tests/` directory (pytest + vitest) and `vitest.config.js`; removed vitest/jsdom from `package.json`.
- Deleted 6 tracked `data/backups/*.db` files; gitignored `data/backups/` and `agent/memory.json`.
- Removed unwired migration utilities: `migration_runner.py`, `migrate_schema.py`, `migrate_sessions.py`, `migrate_tie_breaker.py`.
- Replaced outdated root `EPC17_WORKFLOW.md` with accurate `docs/EPC17_WORKFLOW.md`; added `docs/EPC17_PROMPTS.md`.
- Deleted superseded `docs/HISTORY.md` and `docs/TESTING.md`.
- Updated `README.md` (directory tree, license badge, removed test/migration sections).
- Updated `.cursor/rules/EPC17_RULES.mdc` for actual `js/`/`modules/`/`utils/` layout.
- Fixed `requirements.txt` (removed invalid `uuid` pip entry); synced `package.json` version to 2.8.4.

### Files
- tests/ (deleted), vitest.config.js, data/backups/*.db, agent/memory.json
- utils/migration_runner.py, utils/migrate_schema.py, utils/migrate_sessions.py, utils/migrate_tie_breaker.py
- EPC17_WORKFLOW.md, docs/HISTORY.md, docs/TESTING.md
- docs/EPC17_WORKFLOW.md, docs/EPC17_PROMPTS.md
- README.md, .cursor/rules/EPC17_RULES.mdc, requirements.txt, package.json, package-lock.json, .gitignore

### Meta
Source: LLM | Session: Repository Cleanup

---

## [2.8.3] - 2026-05-30 - patch

### Summary
UI polish: sidebar scroll, shared loading skeletons, registration/participants UX, modal behavior, events cleanup, analytics podium fix, final-results picker.

### Details
- Registration inline existing-driver panel scroll fix; sidebar flex scroll + FA 6.5.1 sitewide; nav group visual hierarchy.
- Shared `UIComponents.showListSkeleton` + cv-skeleton-grid on participants, existing-drivers, final-results, driver-profile.
- Registration: short event labels, first/last name, optional DOB, split emergency contact, compact vehicle inline animation.
- Participants: collapsible events/classes on cards; edit modal single-event picker dropdown; backdrop close with dirty confirm.
- High-contrast `.cv-select`; centered tech inspection header.
- `Helpers.showModal` backdrop close + dirty confirm; series modal Close button + scrollable modal body.
- Events: removed dual class drag UI section, maxParticipants, driverMeeting; unlimited registration in db.js.
- Analytics podiums count class final top-3 per event (not heat finishes); test in test_stats_counts.py.
- Final results hero event jump dropdown synced with card grid.

### Files
- styles.css, styles/pages/participants.css, utils/ui-components.js, utils/helpers.js, utils/data-manager.js, utils/db.js, utils/db_manager.py, server.py
- registration.html, components/registration-form.html, participants.html, existing-drivers.html, final-results.html, driver-profile.html, tech-inspection.html, events.html, modules/series.js, js/sidebar-nav.js
- tests/test_stats_counts.py, version.json

### Meta
Source: LLM | Session: UI Polish 2.8.3

---

## [2.8.2] - 2026-05-30 - patch

### Summary
Fix completed heat stats, Carbon Velocity sidebar, inline duplicate registration, and unified participant event-class editing.

### Details
- **Stats fix:** `count_completed_heats_from_brackets()` walks `race_brackets` JSON heats; `/api/stats/overall` returns `totalRaces` and `completedRaces`; home page reads corrected field. Unit tests in `tests/test_stats_counts.py`.
- **Sidebar:** Carbon Velocity styling (accent bar, icon wells, group headers); FA 6.5-aligned icons in `js/sidebar-nav.js`.
- **Registration duplicate flow:** New `modules/existing-driver-registration.js`; duplicate banner opens inline panel with class picker — no redirect to existing-drivers (optional link retained).
- **Participants edit modal:** Replaced separate Racing Classes + Event Assignments with unified Event Registrations cards driven by `eventClasses`; computed total fee from class prices; clearer status labels; styles moved to `styles/pages/participants.css`.

### Files
- utils/db_manager.py, server.py, index.html, tests/test_stats_counts.py
- styles.css, js/sidebar-nav.js
- modules/existing-driver-registration.js, registration.html
- participants.html, styles/pages/participants.css
- version.json

### Meta
Source: LLM | Session: Stats, Sidebar, Registration Flow, and Participant Edit

---

## [2.8.1] - 2026-05-30 - patch

### Summary
Refined Carbon Velocity visuals: replaced harsh WebGL streak background, rebuilt home landing page, toned down panel/animation excess.

### Details
- Replaced WebGL speed-streak shader with subtle CSS mesh grid + optional soft orb canvas (home only).
- `init.js` / `motion.js`: page-aware motion; hover lift only on tiles/cards, not every panel.
- `hud-panel` simplified (no laser line on all panels); `hud-panel-accent` for highlights only.
- Typography: display headings use Plus Jakarta Sans; racing font reserved for stats/hero accents.
- Font Awesome upgraded to 6.5.1 via `cv-load.js`.
- Home page fully rebuilt: cinematic hero + live preview card, quick-action grid, workflow steps, rich event list, feature grid, CTA.
- New `styles/pages/home.css` for home-specific layout.

### Files
- js/fx/velocity-bg.js, js/fx/init.js, js/fx/motion.js, js/cv-load.js
- styles.css, styles/pages/home.css
- index.html, login.html
- version.json

### Meta
Source: LLM | Session: Carbon Velocity visual refinement

---

## [2.8.0] - 2026-05-30 - minor

### Summary
Carbon Velocity UI remaster — new racing-HUD theme, WebGL backgrounds, GSAP motion, and consistent page chrome across the app.

### Details
- Replaced Hyper-Tech design tokens with **Carbon Velocity** palette (carbon black surfaces, electric blue, magenta, ignition orange) in `styles.css`.
- Added shared HUD components: `.hud-panel`, `.cv-hero`, `.cv-stats-grid`, `.cv-feature-grid`, `.cv-tabs`, series cards, registration compact layout, tech inspection, final-results podium rows.
- New visual FX layer: `js/fx/velocity-bg.js` (WebGL speed-line shader + CSS fallback), `js/fx/motion.js` (GSAP reveals), `js/fx/counters.js`, `js/fx/init.js`, `js/cv-load.js` (GSAP CDN loader).
- Rebuilt `index.html` home dashboard with live KPI stats from `/api/stats/overall` and upcoming events strip.
- Modernized page heroes: registration, existing-drivers, participants, driver-profile, series, events, analytics, final-results, tech-inspection, users, login.
- Registration: single-screen HUD console, inline per-class vehicle fields (no scroll overflow), link to existing-drivers, `sledConfigurations` fix.
- Participants: View Profile action linking to `driver-profile.html?driver=`.
- Driver profile: deep-link via `?driver=` URL param.
- Series: removed dev Clear/Debug buttons, fixed `.series-card` CSS, cv-stat header.
- Chart.js theme updated in `utils/chart-helpers.js` for Carbon Velocity colors.
- Permission fixes: `participants.html`, `existing-drivers.html`, `final-results.html` added to server and auth nav maps.
- DB: `team` and `notes` columns on participants; `vehicleConfigurations` normalized to `sledConfigurations` in DataManager.
- Races page structure unchanged; colors inherit from new tokens + existing `styles/race.css`.
- Out of scope (deferred): live-display, animator.

### Files
- styles.css
- js/fx/velocity-bg.js, js/fx/motion.js, js/fx/counters.js, js/fx/init.js, js/cv-load.js
- js/sidebar-nav.js
- utils/chart-helpers.js, utils/auth.js, utils/data-manager.js, utils/db_manager.py
- server.py
- index.html, login.html, registration.html, existing-drivers.html, participants.html, driver-profile.html
- tech-inspection.html, series.html, events.html, final-results.html, analytics.html, users.html
- components/registration-form.html
- version.json

### Meta
Source: LLM | Session: Carbon Velocity Remaster

---

### Summary
Rebuilt Tech Inspection into a hybrid event-first page with per-driver history across events.

### Details
- Replaced `tech-inspection.html` with a minimal, stable shell and no inline controller logic.
- Implemented a fresh `js/tech-inspection.js` controller that loads events, resolves event drivers, renders inspections, and updates summary counters.
- Added localStorage-backed hybrid model:
  - `epc17_inspection_by_event` for event + driver records
  - `epc17_inspection_history_by_driver` for cross-event driver history
- Added idempotent migration from legacy `epc17_inspection_data` into the new structures.
- Added a dedicated driver history panel for the selected driver and status upsert per event.
- Kept page auth/event access aligned with existing registration permission flow.

### Files
- tech-inspection.html
- js/tech-inspection.js
- PATCHNOTES.txt
- version.json
- CHANGELOG.md (via generate_changelog.py)

### Meta
Source: LLM

---

## [2.7.22] - 2026-05-27 - patch

### Summary
Tech Inspection: Flask now serves HTML from disk so a long-running server no longer keeps a stale Jinja-cached page.

### Details
- Root cause: server ran with `debug=off`; `render_template('tech-inspection.html')` kept the pre-fix template (with `dataManager.initialize()`) in memory while disk had the fixed file.
- `tech_inspection_page` uses `_read_html_page()` + no-cache headers instead of `render_template`.
- Enabled `TEMPLATES_AUTO_RELOAD` by default for local dev (`EPC17_DEV` not `0`).

### Files
- server.py
- tech-inspection.html
- js/tech-inspection.js
- PATCHNOTES.txt
- version.json
- CHANGELOG.md (via generate_changelog.py)

### Meta
Source: LLM

---

## [2.7.21] - 2026-05-27 - patch

### Summary
Tech Inspection: external JS bundle and no-cache HTML so browsers stop running the old `initialize()` inline script.

### Details
- Moved page controller to `js/tech-inspection.js?v=2.7.21` (fixes stale cached inline script still calling `dataManager.initialize()`).
- Event dropdown uses `getEventsArray()` after `loadFromStorage`, with API fallback (limit 500).
- Flask route: `registration` permission, no-cache headers, `/tech-inspection` alias; added to static HTML permission map.

### Files
- tech-inspection.html
- js/tech-inspection.js
- server.py
- PATCHNOTES.txt
- version.json
- CHANGELOG.md (via generate_changelog.py)

### Meta
Source: LLM

---

## [2.7.20] - 2026-05-27 - patch

### Summary
Fixed Tech Inspection page: event dropdown and driver list now load after correct DataManager initialization.

### Details
- Replaced invalid `dataManager.initialize()` with `waitForAuthentication()` and `loadFromStorage(['participants', 'events'])` (root cause of empty event dropdown).
- Event select: sorted by date, empty/error states, duplicate-safe repopulation.
- Driver list: `loadParticipantsForEvent` fallback when event participant IDs are missing; ID-normalized event lookup; sanitized display names.
- Inspection data scoped per event in localStorage (`epc17_inspection_{eventId}`) with one-time migration from legacy global key.
- Summary stats: separate passed, failed, and pending counts; added Failed column.
- `Helpers.parseSelectedClasses` and `Helpers.computeInspectionSummary` for shared logic; Vitest coverage.
- Tech Inspection nav gated to `registration` permission (same as Registration).

### Files
- tech-inspection.html
- utils/helpers.js
- utils/auth.js
- tests/inspection-summary.test.js
- PATCHNOTES.txt
- version.json
- CHANGELOG.md (via generate_changelog.py)

### Meta
Source: LLM

---

## [2.7.19] - 2026-05-27 - patch

### Summary
Fixed lower-bracket reset/re-score leaving the new winner out of the next generated round.

### Details
- `reverseParticipantRecords` did not restore `status` or `currentBracket`, so a driver eliminated before a reset stayed eliminated when they won on re-entry.
- Added `rebuildParticipantStateFromCompletedHeats` to recompute all participant records from completed heats (used on reset, regenerate later rounds, and generate next round).
- Winners in double elimination now explicitly stay `active` and keep the correct bracket (`upper`/`lower`) from the heat they won.
- Tests cover rebuild after reset and lower-bracket winner inclusion.

### Files
- modules/race.js
- tests/race-reset-safety.test.js
- PATCHNOTES.txt
- version.json
- CHANGELOG.md (via generate_changelog.py)

### Meta
Source: LLM

---

## [2.7.18] - 2026-05-27 - patch

### Summary
Later rounds refresh when an earlier race is reset and re-scored; stale pending rounds are removed on reset.

### Details
- Resetting a completed heat now removes later rounds that have no completed races yet (avoids outdated pairings).
- Recording a result in an earlier round regenerates pending later rounds (e.g. Round 4 pairings update after a Round 3 correction).
- Toasts inform operators when later rounds were removed or refreshed.
- Added tests for `removeLaterPendingRounds`.

### Files
- modules/race.js
- js/race-ui.js
- tests/race-reset-safety.test.js
- PATCHNOTES.txt
- version.json
- CHANGELOG.md (via generate_changelog.py)

### Meta
Source: LLM

---

## [2.7.17] - 2026-05-27 - patch

### Summary
Fixed completed race heats falsely showing a lock icon; reset stays available until a later round has completed races.

### Details
- `canSafelyResetHeat` called async `getHeat`/`getBracket` without `await`, so every completed heat failed the safety check and appeared locked immediately after completion.
- Added sync bracket resolution (`resolveSyncBracket`, `findHeatInBracket`) for render-time safety checks.
- Later-round lock now applies only when a later round has at least one completed heat (pending Round 4 no longer blocks Round 3 resets).
- `resetCompletedHeat` and `resetRoundAndSubsequentRounds` now await bracket/heat loading correctly.
- Legacy vertical `renderHeat` shows reset/lock on completed heats (aligned with horizontal layout).
- Races page re-renders brackets after each heat save so reset controls update promptly.
- Added `tests/race-reset-safety.test.js`.

### Files
- modules/race.js
- js/race-ui.js
- tests/race-reset-safety.test.js
- PATCHNOTES.txt
- version.json
- CHANGELOG.md (via generate_changelog.py)

### Meta
Source: LLM

---

## [2.7.16] - 2026-05-27 - patch

### Summary
Smaller toast notifications app-wide with 1.5× longer display time.

### Details
- `TOAST_DURATION_MS` set to 7500 (was 3000 unused; runtime was 5000 ms).
- `Helpers.showToast` uses the constant (fallback 7500), compact padding (8px 12px), max-width 220px, font-size 0.7rem; theme `.toast-*` classes apply (removed legacy inline background colors).
- Global `.toast` / `#toast-container` styles updated to match.
- Registration and driver-profile page-local toasts aligned (4500 ms, same compact sizing).

### Files
- utils/constants.js
- utils/helpers.js
- styles.css
- registration.html
- driver-profile.html
- PATCHNOTES.txt
- version.json
- CHANGELOG.md (via generate_changelog.py)

### Meta
Source: LLM

---

## [2.7.15] - 2026-05-27 - patch

### Summary
Fixed cross-heat participant swap failing with false "different rounds" errors when drivers still appeared in completed earlier-round heats.

### Details
- `handleSwapParticipant` now carries `heatId`, `laneNumber`, and `bracketType` from the swap picker so the target heat matches what the user selected (e.g. lower-bracket Race #26 in Round 2).
- Swap candidates are limited to the same `bracketType` as the source heat in double elimination.
- Participant selection dialog shows race number in labels when available (`Race #26 – Driver Name`).
- `findParticipantHeat` accepts optional filters: class, round, exclude completed, bracket type; prefers current round first.
- `getHeatRound` prefers round encoded in heat id (`heat-r{n}-h{m}-...`); `resolveHeatRound` uses heat metadata before bracket container lookup.
- `performCrossHeatSwap` and `moveParticipantToOtherHeat` use `resolveHeatRound` on resolved heat objects.
- Added unit tests for duplicate participant across Round 1 (completed) vs Round 2 (active).

### Files
- js/race-ui.js
- tests/race-ui-heat-round.test.js
- PATCHNOTES.txt
- version.json
- CHANGELOG.md (via generate_changelog.py)

### Meta
Source: LLM

---

## [2.7.14] - 2026-05-27 - patch

### Summary
Fixed desktop first click being swallowed by an invisible sidebar overlay.

### Details
- `openSidebar()` now activates the backdrop overlay only on mobile (≤768px); desktop no longer gets a full-screen transparent layer with `pointer-events: auto` on load.
- `updateLayout()` on desktop calls `closeSidebar()` instead of `openSidebar()` so overlay and `sidebar-open` body state are cleared on init and resize.
- `updateSidebarState()` re-applies overlay only when mobile drawer is open.
- CSS: `pointer-events: auto` on `.sidebar-overlay.active` limited to mobile; desktop and collapsed sidebar force `pointer-events: none`.

### Files
- js/sidebar-nav.js
- styles.css
- PATCHNOTES.txt
- version.json
- CHANGELOG.md (via generate_changelog.py)

### Meta
Source: LLM | Session: fix desktop first-click overlay

---

## [2.7.13] - 2026-05-27 - patch

### Summary
Fixed finish-position click toggling after False Start/DSQ and show event race numbers in Move to Other Heat.

### Details
- Removed duplicate per-participant click listeners added on heat refresh; document-level delegation now handles position clicks exclusively (fixes assign/unassign firing twice after FS or DSQ).
- FS/DSQ results stay on heat.results only; pendingResults seeding and slot math use manual positions only.
- effectiveMaxPosition and shouldAutoCompleteHeat account for FS and DSQ when counting finish slots.
- Move to Other Heat dialog and toasts use event-wide Race #N (raceNumber) instead of per-class heatNumber.

### Files
- js/race-ui.js
- PATCHNOTES.txt
- version.json
- CHANGELOG.md (via generate_changelog.py)

### Meta
Source: LLM | Session: fix FS position toggling and move heat labels

---

## [2.7.12] - 2026-05-26 - patch

### Summary
Retuned the API data simulator for a cleaner 30-event / 2-series dataset with realistic per-event scale and stronger analytics validation.

### Details
- Added `GenerationConfig` as the single source of truth for scale (30 events, 2 series, 180-driver pool, 40-80 drivers and 50-120 registrations per event, 3-6 classes).
- Rewrote event class selection for bounded class counts per archetype; pre-flight eligibility against the driver pool before specs are finalized.
- Orchestrates two series (Northern/Southern) with a shared global driver pool and cumulative `eventClasses` across events.
- Expanded post-generation validation: per-event bounds, multi-event driver overlap, and analytics smoke checks (overall, event, series, driver, lane).
- Improved `--dry-run` summary table; removed hardcoded 40-event guard; fixed nondeterministic `rng` usage in event payloads.
- Rewrote `tests/test_generate_data.py` and updated `tests/test_generate_data_issue123.py` for new rules.

### Files
- utils/generate_data.py
- tests/test_generate_data.py
- tests/test_generate_data_issue123.py
- README.md
- PATCHNOTES.txt
- version.json
- CHANGELOG.md (via generate_changelog.py)

### Meta
Source: LLM | Session: improve data generation simulator

---

## [2.7.11] - 2026-05-26 - patch

### Summary
Improved collapsed (icon-rail) sidebar: click-open flyouts, compact footer, clearer tooltips and active states.

### Details
- Collapsed groups open on click with fixed-position flyout panels (no hover-only); close on outside click or Escape; reposition on resize.
- Icon rail uses 48px targets, lime active/hover accents, and title tooltips for Home and group headers.
- Footer stays visible when collapsed (icon logout, admin Users icon, version hidden).
- Flyouts use rounded panel styling and scroll when tall; expanding sidebar clears open flyouts.

### Files
- js/sidebar-nav.js
- styles.css
- utils/auth.js
- PATCHNOTES.txt
- version.json
- CHANGELOG.md (via generate_changelog.py)

### Meta
Source: LLM | Session: collapsed sidebar UX

---

## [2.7.10] - 2026-05-26 - patch

### Summary
Sidebar navigation uses acid-lime accent (Visual group style) across all groups and links.

### Details
- Group header icons, chevrons, nested link hovers/active states, and flyouts use `--secondary` / `--laser-secondary` instead of cyan primary accents.
- Removed Visual-only icon override; nested flyout links share group item styling.

### Files
- styles.css
- PATCHNOTES.txt
- version.json
- CHANGELOG.md (via generate_changelog.py)

### Meta
Source: LLM | Session: sidebar lime accent unification

---

## [2.7.9] - 2026-05-26 - patch

### Summary
Fix duplicate sidebar links caused by collapsed-mode flyout menus showing while sidebar is expanded.

### Details
- `.nav-group-flyout` is now `display: none` by default; flyouts only appear when `.sidebar.collapsed` (hover/focus).

### Files
- styles.css
- PATCHNOTES.txt
- version.json
- CHANGELOG.md (via generate_changelog.py)

### Meta
Source: LLM | Session: sidebar duplicate links fix

---

## [2.7.8] - 2026-05-26 - patch

### Summary
Retired event-analytics page; reorganized sidebar (Analytics under Events, new Visual group) with modernized navigation UX.

### Details
- Removed `event-analytics.html`; `/event-analytics.html` redirects to `analytics.html` (analytics permission).
- Sidebar: Analytics link moved into Events dropdown; new Visual group for Live Display and Animator.
- Sidebar UX: multi-expand groups with localStorage persistence, ARIA on group headers, active-parent styling, collapsed flyout submenus, flex scroll layout.
- Auth: permission gates apply to sidebar and flyout links; admin Users link in sidebar footer; animator permission on nav map.
- Removed Vite entry for event-analytics.

### Files
- event-analytics.html (deleted)
- js/sidebar-nav.js
- styles.css
- utils/auth.js
- server.py
- vite.config.js
- routes/pages.py
- styles/pages/analytics.css
- PATCHNOTES.txt
- version.json
- CHANGELOG.md (via generate_changelog.py)

### Meta
Source: LLM | Session: sidebar nav refresh

---

## [2.7.7] - 2026-05-26 - patch

### Summary
Remove temporary debug-session instrumentation after race-brackets 500 fix verified.

### Details
- Removed `_agent_debug_log` and NDJSON writes from `utils/db_manager.py`.
- Removed debug logging block from `get_race_brackets` in `server.py`.
- Kept `race_brackets` corruption repair and retry logic.

### Files
- utils/db_manager.py
- server.py
- PATCHNOTES.txt
- version.json
- CHANGELOG.md (via generate_changelog.py)

### Meta
Source: LLM | Session: instrumentation cleanup post race-brackets fix

---

## [2.7.6] - 2026-05-26 - patch

### Summary
Auto-repair corrupt `race_brackets` table so `GET /api/race-brackets` no longer returns 500.

### Details
- Same pattern as series: corrupt SQLite pages made `SELECT id, eventId` / `COUNT(*)` fail with `database disk image is malformed`.
- On startup and on first failed read: `ALTER TABLE … RENAME`, recreate table, restore placeholder rows from salvaged ids and `races.bracketId`/`eventId` pairs.
- `get_race_bracket_metadata` and `get_race_brackets` retry once after repair.

### Files
- utils/db_manager.py
- server.py
- PATCHNOTES.txt
- version.json
- CHANGELOG.md (via generate_changelog.py)

### Meta
Source: LLM | Session: race-brackets 500 / table corruption repair

---

## [2.7.5] - 2026-05-26 - patch

### Summary
Remove temporary debug-session instrumentation (`_agent_debug_log`, `_agent_debug_log_api`, NDJSON log files).

### Details
- Stripped agent debug logging from `utils/db_manager.py` and `server.py` after series 500 fix was verified.
- Kept SQLite corruption prevention and series-table repair logic; errors use standard `logger` / `print` + traceback.

### Files
- utils/db_manager.py
- server.py
- PATCHNOTES.txt
- version.json
- CHANGELOG.md (via generate_changelog.py)

### Meta
Source: LLM | Session: instrumentation cleanup post-fix

---

## [2.7.4] - 2026-05-26 - patch

### Summary
Prevent SQLite corruption (safe backups, WAL cleanup, no dev reloader); auto-rebuild corrupt/missing `series` table so `/api/series` stays up.

### Details
- **Root cause (runtime):** `series` table row storage was corrupt (`database disk image is malformed`); `DROP TABLE` failed; `ALTER TABLE … RENAME` + fresh `CREATE` works.
- **Prevention:** Backups use `connection.backup()` after `PRAGMA wal_checkpoint(TRUNCATE)` instead of `shutil.copy2` on a live WAL database. Clear-database removes `.db`, `-wal`, and `-shm`. `busy_timeout`, `wal_autocheckpoint`, checkpoint on `close()`, `atexit` shutdown hook. Flask/SocketIO `use_reloader=False` to avoid two processes opening the same DB.
- **Recovery:** On startup, create `series` if missing; if unreadable, rename corrupt table and recreate (placeholder rows from event `seriesId` when possible).

### Files
- utils/db_manager.py
- server.py
- PATCHNOTES.txt
- version.json
- CHANGELOG.md (via generate_changelog.py)

### Meta
Source: LLM | Session: series 500 / SQLite corruption prevention

---

## [2.7.3] - 2026-03-20 - patch

### Summary
Add targeted runtime instrumentation for `/api/series` so authenticated 500s can be narrowed to SQL, row transformation, or response serialization using live evidence.

### Details
- `DatabaseManager.get_series()` now logs `db_get_series_start`, row count, row-level failures, and query-level failures to `debug-be91c4.log` with hypotheses `H5`/`H6`.
- `/api/series` keeps existing traceback logging and now returns a temporary `debug` payload when called with `?debug=be91c4`, allowing direct authenticated repro from the terminal without relying on locked log file reads.

### Files
- utils/db_manager.py
- server.py
- PATCHNOTES.txt
- version.json
- CHANGELOG.md (via generate_changelog.py)

### Meta
Source: LLM | Session: API 500 series targeted runtime instrumentation

---

## [2.7.2] - 2026-03-20 - patch

### Summary
Harden SQLite startup after index repair (`quick_check` + optional `REINDEX`); log tracebacks for `/api/series`, `/api/race-brackets`, and stats routes into `debug-be91c4.log`; safer pagination query parsing on series and brackets.

### Details
- After successful DB init loop: run `PRAGMA quick_check`; if not `ok` or any index repair ran (`repairs_done > 0`), run `REINDEX` (errors logged to NDJSON, hypothesis H2).
- `handle_series` GET, `get_race_brackets`, `get_overall_analytics`, `get_driver_analytics`: on exception append full traceback via `_agent_debug_log_api` (H3/H4) for LAN diagnosis without console access.
- Series GET and race-brackets GET: `page` / `limit` tolerate non-numeric query params (avoid uncaught `ValueError` → 500).

### Files
- utils/db_manager.py
- server.py
- PATCHNOTES.txt
- version.json
- CHANGELOG.md (via generate_changelog.py)

### Meta
Source: LLM | Session: API 500 series/brackets/stats follow-up

---

## [2.7.1] - 2026-03-20 - patch

### Summary
Fix API 500 errors when SQLite reports corrupt indexes (`malformed database schema` / invalid rootpage) by repairing schema entries and recreating indexes on startup.

### Details
- Observed: `GET /api/participants`, `/api/events`, `/api/series` returned 500 because `DatabaseManager` failed during `PRAGMA journal_mode=WAL` with errors such as `malformed database schema (idx_participants_status) - invalid rootpage`.
- `DROP INDEX` on a corrupt index often fails with the same error; recovery now uses `PRAGMA writable_schema=ON`, deletes the bad row from `sqlite_master`, reconnects, and retries (up to 20 indexes) until init succeeds, then `_ensure_indexes()` rebuilds indexes.
- Optional NDJSON lines append to `debug-be91c4.log` when repair runs (session `be91c4`) for field diagnosis.

### Files
- utils/db_manager.py
- PATCHNOTES.txt
- version.json
- CHANGELOG.md (via generate_changelog.py)

### Meta
Source: LLM | Session: API 500 corrupt SQLite index recovery

---

## [2.7.0] - 2026-03-20 - minor

### Summary
Hyper-Tech Precision UI rework: new design system (obsidian surfaces, electric cyan + acid lime, zero radius, laser edges, data typography) across global CSS, navigation shell, race styles, and HTML pages.

### Details
- Replaced legacy orange/glass/rounded tokens in styles.css with Hyper-Tech palette, Plus Jakarta Sans / Inter / Space Grotesk variables, tonal surfaces, 50ms control transitions, LCD-style aura shadows, and utility classes (laser-top, scanlines, module headers, font-data).
- Restyled shared primitives: sidebar/top-nav, buttons (primary lime, outline laser), form controls (bottom stroke), cards, tables, tabs, modals, toasts, badges, pagination, combo-box inputs, skeletons, page/hero patterns.
- styles/race.css aligned to the same system (event cards, section headers, zero radius bulk pass).
- index.html and login.html: replaced large inline marketing/glass styles with dense instrument-style layouts; home hero readout + left-aligned grids.
- live-display.html: flat surface background, content panels use laser-edge modules; bulk cyan/radius normalization.
- Other HTML: Google Fonts link unified to Inter + Plus Jakarta Sans + Space Grotesk; legacy orange RGBA swapped where present.
- js/sidebar-nav.js: body class ht-precision-shell for shell hooks; ui-components.js loading block uses loading-state-ht (left-aligned).

### Files
- styles.css
- styles/race.css
- index.html
- login.html
- live-display.html
- analytics.html
- event-analytics.html
- animator.html
- series.html
- events.html
- registration.html
- participants.html
- races.html
- final-results.html
- existing-drivers.html
- driver-profile.html
- tech-inspection.html
- users.html
- js/sidebar-nav.js
- utils/ui-components.js
- PATCHNOTES.txt
- version.json
- CHANGELOG.md (via generate_changelog.py)

### Meta
Source: LLM | Session: Hyper-Tech Precision rework

---

## [2.6.5] - 2026-03-08 - patch

### Summary
Home page mobile: add visible top bar with hamburger and fix sidebar not opening when hamburger is tapped (GitHub #124).

### Details
- On the home page (index.html) the top bar and hamburger were only injected by sidebar-nav.js; the bar could be missing or hard to discover on mobile. The top bar markup is now included in index.html so it is always in the DOM before the script runs.
- Added mobile-only CSS on the home page to force .top-nav and .top-nav-toggle to display on viewports ≤768px so the hamburger is visible and tappable. sidebar-nav.js skips creating a duplicate top-nav when one already exists and still binds the toggle handler.
- Fixed sidebar not opening on mobile: in styles.css the sidebar was display: none in the mobile block with no override for .sidebar.open, so the menu never appeared. Added .sidebar.open { display: block; } in the same mobile block so the drawer shows when the hamburger is tapped.

### Files
- index.html
- styles.css
- PATCHNOTES.txt
- version.json
- CHANGELOG.md (via generate_changelog.py)

### Meta
Source: LLM | Session: GitHub #124 home page hamburger

---

## [2.6.4] - 2026-03-08 - patch

### Summary
Mobile interaction improvements (GitHub #124): fix header overlap, safe areas, touch targets, and consolidate responsive padding for easier mobile navigation.

### Details
- Top bar and top-tabs no longer overlap on mobile: top-tabs are positioned below the top-nav using --app-top-height and --app-top-tabs-height; main content padding accounts for both when .with-top-tabs is present.
- Added CSS variables --app-top-height (60px) and --app-top-tabs-height (52px) and safe-area insets: .top-nav uses padding-top: max(0.5rem, env(safe-area-inset-top)); main-content padding-top uses calc(var(--app-top-height) + env(safe-area-inset-top)); optional padding-bottom: env(safe-area-inset-bottom) for main-content.
- Mobile navigation clarity: sidebar overlay shows a dimmed backdrop (rgba(0,0,0,0.45)) when open for clear tap-to-close affordance; sidebar nav links and nav-group headers/items have min-height 44px on mobile for touch targets.
- Consolidated all mobile .main-content padding-top to a single approach (calc with variables and safe-area); removed conflicting 65px/70px values in other @media blocks.
- Optional: table-wrapper on mobile has a subtle right-edge inset shadow as a scroll hint for horizontal scroll.
- Cleanup: removed unused mobile-nav.js script from all pages (navigation is handled by sidebar-nav.js); legacy .main-nav / .mobile-nav-* CSS remains but is unused.

### Files
- styles.css
- tech-inspection.html
- series.html
- participants.html
- existing-drivers.html
- final-results.html
- events.html
- races.html
- driver-profile.html
- event-analytics.html
- analytics.html
- PATCHNOTES.txt
- version.json
- CHANGELOG.md (via generate_changelog.py)

### Meta
Source: LLM | Session: GitHub #124 mobile interaction

---

## [2.6.3] - 2026-02-22 - patch

### Summary
Refresh the app UI into a lighter racing dashboard theme with non-modal navigation, cleaner controls, and preserved race/event layouts.

### Details
- Updated global design tokens in `styles.css` to align with the new palette direction (`#0F0F0F`, `#1A1A1A`, `#222222`), brighter content text hierarchy, softer structural shadows, and dedicated fast interaction tokens (`120ms` controls, `200ms` sidebar).
- Reworked sidebar styling to remove heavy modal feel: transparent non-dimming overlay behavior, cleaner active/hover states (left orange indicator instead of full gradient fill), and synchronized slide/panel transitions for smoother navigation feedback.
- Refined global button system for a more mechanical and responsive feel: 8px radius controls, simplified interactions (removed shimmer/ripple effects), orange primary actions, and dark outline-style secondary actions.
- Restyled global cards/panels to use darker grey dashboard surfaces with subtle lift, reduced visual noise, and token-driven hover/focus feedback.
- Updated race/event presentation styles while preserving layout structure: `styles/race.css` now uses the new token system for event cards and race cards without changing event/race DOM organization.
- Updated inline hero/button overrides across core pages (`index.html`, `registration.html`, `existing-drivers.html`, `participants.html`, `series.html`, `races.html`, `events.html`, `final-results.html`) so primary/secondary actions and spacing feel consistent while preserving existing layouts.

### Files
- styles.css
- styles/race.css
- index.html
- registration.html
- existing-drivers.html
- participants.html
- series.html
- races.html
- events.html
- final-results.html
- version.json
- CHANGELOG.md
- PATCHNOTES.txt

### Meta
Source: LLM | Session: racing-dashboard-visual-overhaul

---

## [2.6.2] - 2026-02-17 - patch

### Summary
Fix remaining participant edit lookup failures by adding participant-by-id API retrieval and wiring edit pages to use server fallback when cache misses.

### Details
- Added `GET /api/participants/<participant_id>` support in `server.py` so edit UIs can reliably fetch a participant record by ID.
- Added `getParticipantById()` to `utils/data-manager.js` with server fallback and cache refresh, and warmed cache from paginated participant fetches.
- Updated `modules/driver-edit-form.js` to use `dataManager.getParticipantById()` and corrected auth fetch wrapper usage (`Auth.fetch`) for secure participant lookup.
- Updated `participants.html` edit flow to fetch participant by ID when not in local cache and to keep the registration-focused edit modal path active.
- Updated `existing-drivers.html` driver/edit URL-parameter and selection flows to use participant-by-id fallback so valid drivers no longer trip false `Driver not found`.

### Files
- server.py
- utils/data-manager.js
- modules/driver-edit-form.js
- participants.html
- existing-drivers.html
- PATCHNOTES.txt

### Meta
Source: LLM | Session: issue-111-followup-driver-not-found

---

## [2.6.1] - 2026-02-17 - patch

### Summary
Fix issue #111 existing-event registration editing by correcting participant lookup, event-specific class handling, and edit-flow persistence.

### Details
- Fixed `existing-drivers.html` runtime bootstrapping failure by guarding optional global handler exports that were previously referencing undefined symbols (`closeDriverEditModal`, `saveDriverEdit`).
- Normalized participant/event ID comparisons across frontend and backend participant lookup paths to prevent false `Driver not found` / `Participant not found` outcomes when IDs differ by type (string vs number).
- Corrected race class grouping to resolve `eventClasses` using normalized event IDs before falling back to aggregate classes, preventing event-specific class edits from being interpreted as global/all-class assignments.
- Updated registration edit-mode submission to persist event-scoped class changes under `eventClasses[eventId]` and recompute aggregate `selectedClasses` without overwriting other event mappings.
- Hardened DB update semantics so unchanged participant updates still return success when the record exists, avoiding false failure responses on no-op saves.
- Added regression coverage for:
  - participant registration endpoint ID normalization,
  - participant DB unchanged-vs-changed update behavior,
  - race event-class resolution normalization,
  - registration edit-mode event-specific class persistence.

### Files
- existing-drivers.html
- utils/data-manager.js
- server.py
- modules/race.js
- modules/registration.js
- utils/db_manager.py
- tests/test_api_participants.py
- tests/issue111-registration-editing.test.js
- PATCHNOTES.txt

### Meta
Source: LLM | Session: issue-111-registration-editing

---

## [2.6.0] - 2025-02-17 - minor

### Summary
Version system rework: automatic MAJOR.MINOR.PATCH versioning, organised patch notes (detailed + simple publication view), and trace for all changes/LLM input.

### Details
- Replaced custom version scheme (D0L12R*) with simple semantic versioning: MAJOR.MINOR.PATCH (e.g. 2.6.0). Single source of truth: version.json.
- Added scripts/version_bump.py to bump patch (default), minor, or major automatically.
- Restructured patch notes: PATCHNOTES.txt = detailed (documentation) with Summary/Details/Files/Meta; CHANGELOG.md = simple view for publication (user-facing bullets). All changes and LLM input must leave a trace in PATCHNOTES with Meta/Source.
- Added scripts/generate_changelog.py to build CHANGELOG.md from PATCHNOTES (new-format entries only).
- Server /api/version now reads from version.json instead of parsing PATCHNOTES.
- EPC17_RULES.mdc updated with new versioning and patchnote rules. Legacy entries (pre-2.6) kept below for history.

### Files
- version.json
- scripts/version_bump.py
- scripts/generate_changelog.py
- PATCHNOTES.txt
- CHANGELOG.md
- server.py
- .cursor/rules/EPC17_RULES.mdc

### Meta
Source: LLM | Session: version-system-rework

---

# Legacy entries (pre-2.6 — previous format)

## Version D0L12R2.5 (Patch)

### Large Dataset Performance Optimization
- Reworked backend list APIs to use SQL-level pagination/filtering instead of loading whole tables in memory:
  - `/api/participants` now uses paginated DB queries with bounded limits.
  - `/api/events` now uses paginated DB queries with bounded limits and allowed-event scope filtering.
  - `/api/race-brackets` now returns lightweight metadata for list views and full bracket payload only for `eventId` queries.
- Race stats stability fix for metadata-only bracket cache entries:
  - `utils/data-manager.js#getRaceBracket()` now treats cached brackets as usable only when they include `classes`; metadata-only cache hits are bypassed and replaced by an event-scoped fetch (`/api/race-brackets?eventId=...`) that returns full bracket payload.
  - Event-scoped bracket responses are now cached by both `eventId` and bracket `id` to prevent fallback paths from reusing metadata-only records.
  - This resolves `TypeError: Cannot convert undefined or null to object` in `RaceManager.getTournamentStats()` when selecting events after metadata-first list loads.
- Regression fix: restored a missing JSDoc opener above `getRaceBracket()` that caused `Unexpected identifier 'race'` and prevented `DataManager` from loading on `races.html`.
- Debug instrumentation cleanup: removed temporary debug POST probes from `js/race-ui.js`, `modules/race.js`, and `utils/data-manager.js` after runtime verification confirmed the race event selection issue is fixed.
- Added runtime/permanent DB indexes and new aggregation/count query methods in `utils/db_manager.py` to reduce full scans and expensive JSON-heavy loads.
- Optimized `/api/health` to use count queries instead of fetching full entities.
- Reduced analytics N+1 patterns:
  - `/api/stats/event/<event_id>` now queries event participants directly.
  - `/api/stats/driver/<driver_id>` now fetches only brackets relevant to that driver.
  - `/api/stats/time` and `/api/stats/series/<series_id>` now batch participant/bracket retrieval instead of reloading full datasets per event.
- Frontend data-loading improvements:
  - `utils/data-manager.js` now defaults `getParticipants()`/`getEvents()` to server-side pagination and increased cache TTLs for high-volume usage.
  - `existing-drivers.html` now uses server-paginated driver loading with debounced search and prev/next controls.
  - `driver-profile.html` now uses server-side driver search for selector population instead of unbounded preload.
  - `event-analytics.html` now lazy-loads participants/brackets per selected event instead of preloading huge global datasets.
  - `modules/registration.js` now paginates participant list rendering and uses API-compatible class filtering.
  - `modules/event-data-service.js` now batches participant lookup via map before fallback single lookups.
  - `events.html` removed the 10k event fetch pattern and uses a bounded request.
  - *Files*: `utils/db_manager.py`, `server.py`, `utils/data-manager.js`, `existing-drivers.html`, `event-analytics.html`, `driver-profile.html`, `modules/registration.js`, `modules/event-data-service.js`, `events.html`, `PATCHNOTES.txt`

---

## Version D0L12R2.4 (Patch)

### Race Swap Completion Fixes (Debug-Instrumented)
- Fixed an `isEventCompleted` recursion loop by implementing non-recursive completion evaluation in `EventDataService` (based on bracket class completion state) instead of routing back through deprecated `dataManager.isEventCompleted()`.
- Fixed deferred race completion `ReferenceError` by resolving `classBracket` inside `processDeferredTasks()` before tie-break checks and guarding access with optional chaining.
- Updated race completion status checks to prefer `window.eventDataService` directly, preventing deprecated recursion paths from being reintroduced.
- Removed temporary debug instrumentation after post-fix runtime verification confirmed the issue is resolved.
  - *Files*: `modules/event-data-service.js`, `modules/race.js`, `PATCHNOTES.txt`

---

## Version D0L12R2.3 (Patch)

### Issue #123 Realistic Large-Scale Data Simulation
- Rebuilt `utils/generate_data.py` into an API-driven simulator that creates realistic one-series load scenarios through real endpoints instead of direct DB batch inserts.
- Added issue-specific event modeling:
  - 40 events split into completed/upcoming with deterministic archetypes (`high-class`, `low-key`, `balanced-default`).
  - Logical class selection rules (stock removal for high-class events, biggest-class removal for low-key events).
  - Lane/elimination policy tuned for race-management realism (mostly 3 lanes, mostly double elimination, rare 2-lane single-elim high-load events).
- Added stable driver class-profile generation to keep cross-event participation coherent for analytics.
- Added completed-event bracket simulation with realistic multi-round heats (`lanes`, `results`, completion timestamps, race numbers) so stats endpoints consume data like truly raced events.
- Added post-generation validation checks for event split, driver/registration constraints, lane/elimination distribution, and stats endpoint visibility.
- Added focused unit tests for issue-123 generation constraints.
- Updated README with issue-123 simulator usage examples and dry-run command.
  - *Files*: `utils/generate_data.py`, `tests/test_generate_data_issue123.py`, `README.md`, `PATCHNOTES.txt`

---

## Version D0L12R2.2 (Patch)

### Driver Achievements Restored (#122)
- Restored achievements display in Driver Profile by adding a dedicated Achievements section with earned/total summary and badge rendering.
- Added `modules/achievements.js` integration to `driver-profile.html` and wired it into the existing profile load flow.
- Extended `GET /api/stats/driver/<driver_id>` to return an `achievements` payload computed from race brackets, including earned badges and supporting stats.
- Hardened achievements computation for sparse bracket data (works even when participant arrays are incomplete but race results exist).
- Updated automated coverage:
  - `tests/achievements.test.js` now loads the achievements engine via global export in test runtime.
  - `tests/test_stats_driver_profile.py` now validates the new API achievements payload.
- Added missing JS test tooling dependencies for this test suite (`vitest`, `jsdom`).
  - *Files*: `driver-profile.html`, `server.py`, `tests/achievements.test.js`, `tests/test_stats_driver_profile.py`, `README.md`, `package.json`, `package-lock.json`

---

## Version D0L12R2.1 (Patch)

### 🤖 Local GitHub Issue Agent
- Added a new Python `agent/` module to support an API-driven GitHub issue workflow with Cursor-assisted responses.
  - Includes env-based config loading, GitHub issue operations (list/read/comment/label/close), prompt/context builders, and a CLI entrypoint (`python -m agent.cli`).
- Added local action trace logging via `agent/memory.json` to keep a history of automated operations.
- Added tests for prompt building and issue manager flow (`tests/test_agent_context_builder.py`, `tests/test_agent_issue_manager.py`).
- Updated dependency manifest to include `PyGithub` and `requests`.
- Added setup/usage docs in `agent/README.md` and linked quick-start usage in `README.md`.
  - *Files*: `agent/__init__.py`, `agent/config.py`, `agent/github_client.py`, `agent/context_builder.py`, `agent/cursor_interface.py`, `agent/issue_manager.py`, `agent/cli.py`, `agent/memory.json`, `agent/README.md`, `tests/test_agent_context_builder.py`, `tests/test_agent_issue_manager.py`, `requirements.txt`, `README.md`

---

## Version D0L12R2.0 (Major — Integrated Improvement Plan)

### 🐛 Bug Fixes (Phase 0)
- **Class Registration (#6, #7)**: Removed fallback that showed all series classes when an event has no class settings; now displays a clear "No Classes Configured" warning in both existing-drivers.html and registration.js.
- **Event Class Selection Persistence**: Save event class settings with enabled flags and reliable series fallback so only chosen classes stay enabled on create/edit.
  - *Files*: `events.html`
- **Stats Counting Unraced Heats (#8)**: Added `isHeatCompleted()` guard in statistics-manager.js so incomplete heats are no longer counted as losses.
- **Heat Reset Error (#9)**: Fixed `canSafelyResetHeat()` inversion and used `isHeatCompleted()` consistently in `resetCompletedHeat()`. Verified event completion timing.
- **Final Race Ordering (#11)**: Sorted heats by raceNumber before rendering across all bracket render paths in race-ui.js.
- **Final Results Navigation (#12)**: Added "Back to Events" button in final-results.html header.
- **Analytics Fixes (#13, #14, #15)**: Fixed race completion % using actual bracket data, forced horizontal board titles, and confirmed revenue binding correctness.
- **Registration Event Linking**: `dataManager.registerParticipantForEvent()` now calls the server endpoint and updates local caches when `eventDataService` is unavailable.
  - *Files*: `utils/data-manager.js`
  - *Files*: `existing-drivers.html`, `modules/registration.js`, `modules/statistics-manager.js`, `modules/race.js`, `js/race-ui.js`, `final-results.html`, `event-analytics.html`

### ⚙️ Race Engine (Phases 1–2)
- **Tie-breaker Logic (#1)**: Auto-triggers tie-breaker races after finals complete; includes result processing and settings UI (`tieBreakerEnabled`, `tieBreakerRank`) in event form.
- **Manual Loss / Un-DQ (#10)**: Added `addManualLoss`, `removeManualLoss`, `undoDisqualification` to RaceManager with context menu integration.
- **SVG Visual Bracket Tree (#2)**: New toggle between List View and Tree View in race-ui.js; tree rendered via `js/race-ui/bracket-tree.js`.
- **Module Split (stubs)**: Created `modules/race/` (bracket-builder, elimination-engine, result-processor, tie-breaker, final-results) and `js/race-ui/` (8 sub-modules) with documented migration targets.
  - *Files*: `modules/race.js`, `js/race-ui.js`, `modules/event-ui.js`, `modules/event-controller.js`, `races.html`, `js/race-ui/bracket-tree.js`, `modules/race/*`, `js/race-ui/*`

### 📄 New Modules (Phases 3–4)
- **Crash-proof Event Snapshot (#3)**: `modules/event-snapshot.js` — generates printable event snapshots with auto-print option on EventBus updates.
- **Purse Calculator (#4)**: `modules/purse-calculator.js` — pool config, per-class/injected money, position payouts, and summary HTML.
- **Tech Inspection Page (#5)**: `tech-inspection.html` — event selection, driver checklist, summary stats, localStorage persistence. Added sidebar nav link and Flask route.
- **Data Manager Split (stubs)**: Created `utils/data-manager/` (6 domain sub-modules) with documented migration targets. Slimmed deprecated methods in `utils/data-manager.js`.
  - *Files*: `modules/event-snapshot.js`, `modules/purse-calculator.js`, `tech-inspection.html`, `server.py`, `js/sidebar-nav.js`, `utils/data-manager.js`, `utils/data-manager/*`

### 🏗️ Backend (Phase 5)
- **Flask Blueprint Stubs**: Created `routes/` directory with 10 blueprint stub files + 2 helper modules (`utils/server_helpers.py`, `utils/auth_helpers.py`).
- **Migration Runner**: `utils/migration_runner.py` — versioned DB migration with `_migrations` table, auto-discovery, server-startup integration.
- **DB Logging**: Replaced `print()` statements in `utils/db_manager.py` with Python `logging` module calls.
  - *Files*: `routes/*`, `utils/server_helpers.py`, `utils/auth_helpers.py`, `utils/migration_runner.py`, `utils/db_manager.py`

### 🔧 Pairing & Achievements (Phase 6)
- **Pairing Engine Split (stubs)**: Created `modules/pairing/` (lane-assigner, opponent-tracker, heat-generator) with documented migration targets.
- **Driver Achievements (#16)**: `modules/achievements.js` — 10 achievement types with check functions, badge rendering.
  - *Files*: `modules/pairing/*`, `modules/achievements.js`

### 📦 Tooling & Infrastructure (Phases 7–8)
- **Vite Bundler**: `vite.config.js` — multi-page entry points, dev proxy to Flask backend.
- **ESLint + Prettier**: `.eslintrc.json`, `.prettierrc.json` with project globals.
- **Constants**: `utils/constants.js` — extracted magic numbers (breakpoints, timing, race engine, UI).
- **Vitest**: `vitest.config.js` + `tests/purse-calculator.test.js`, `tests/achievements.test.js`.
- **Pytest**: `tests/conftest.py`, `tests/test_api_participants.py`, `tests/test_api_events.py`.
- **CSS Split**: `styles/print.css` (print media styles), `styles/tokens.css`, `styles/components.css` (stubs), `styles/pages/` (8 page-specific CSS stubs).
- **Lazy Charts**: Added `lazyChart()` IntersectionObserver helper to `utils/chart-helpers.js`; integrated into `event-analytics.html` for all 3 charts.
  - *Files*: `vite.config.js`, `package.json`, `.eslintrc.json`, `.prettierrc.json`, `utils/constants.js`, `vitest.config.js`, `tests/*`, `styles/*`, `utils/chart-helpers.js`, `event-analytics.html`

---

## Version D0L12R1.12 (Patch)

### 📊 Analytics
- **Contextual Lane Performance (Effective)**: Added context-aware lane metrics that preserve existing **Raw Stats (Unadjusted)** while layering in: solo-race exclusion, expected wins baseline (\(1 / activeLanes\)), performance index (actual/expected), effective usage share, and confidence/exclusion tagging for small samples. Displayed both overall (Lane Performance tab) and per-event (Events tab).
  - *Files*: `server.py`, `analytics.html`, `tests/test_stats_lane_contextual.py`

---

## Version D0L12R1.8 (Patch)

### 🎬 Animator
- **Stability + Accuracy Fixes**: Fixed the "Since last race" timer resetting on refresh by correcting completion detection (empty results no longer treated as finished). Recent Results now only shows completed heats, and the header "heats left" now reflects **event remaining** (with current class context).
  - *Files*: `modules/animator-controller.js`, `modules/animator-utils.js`, `modules/animator-renderer.js`
- **Better Driver Cards**: Reduced current-race card size and added a **hover/focus details overlay** (overall stats, win rate, avg position, best streak, recent form, sled info when available).
  - *Files*: `animator.html`, `modules/animator-renderer.js`, `modules/animator-utils.js`
- **Smoother Feel**: Debounced bursty WebSocket refreshes, optimized participant lookups via an ID map, and avoided re-animating/re-rendering side lists when content hasn't changed.
  - *Files*: `modules/animator-controller.js`, `modules/animator-renderer.js`, `modules/animator-utils.js`

---

## Version D0L12R1.9 (Patch)

### 🎬 Animator
- **Instant Result Updates + Cinematic Flow**: Animator now refreshes immediately on bracket/result updates (Socket.IO-first, with a fast 3s fallback poll only when WS is unavailable). When a heat completes, the center panel performs an **in-place finish-order reveal** (all lanes), then the completed heat **slides into Recent Results** and the next heat **promotes from Up Next into center** using a floating layer so cards don't "teleport".
  - *Files*: `animator.html`, `modules/animator-controller.js`, `modules/animator-renderer.js`

---

## Version D0L12R1.10 (Patch)

### 🎬 Animator
- **UI Polish + Readability**: Removed the "Updated: … ago" status pill (cleaner broadcast UI), added clear **finish-position badges/colors** during result reveals, and expanded driver info (last race class + richer career context). Also prevented periodic refreshes from resetting hover/expanded states by skipping DOM rebuilds when the displayed content hasn't changed.
  - *Files*: `animator.html`, `modules/animator-controller.js`, `modules/animator-renderer.js`, `modules/animator-utils.js`

---

## Version D0L12R1.7 (Patch)

### 🎬 Animator
- **Live Animator Dashboard Rework**: Made `animator.html` consistently functional and "live" by bootstrapping the missing data layer, forcing fresh bracket loads on real-time events, and improving driver name resolution. Added **AUTO vs PINNED** mode (click Up Next to pin; `L` to return to live) plus **Recent Results replay** (click results or `1/2/3`). Improved GSAP transitions and aligned visuals to the app's glass UI styling.
  - *Files*: `animator.html`, `modules/animator-controller.js`, `modules/animator-renderer.js`, `modules/animator-utils.js`

---

## Version D0L12R1.6 (Patch)

### 🖥️ Live Display
- **Compact Hero + No Controls**: Reduced the Live Display hero header height and removed the Refresh/Fullscreen buttons so the only on-screen control is the small Back arrow.
  - *File*: `live-display.html`

---

## Version D0L12R1.5 (Patch)

### 🖥️ Live Display
- **Visual Refresh + Controls**: Improved Live Display visuals to better match EPC17's glass UI (spacing, card contrast, in-progress highlighting, focus visibility, reduced-motion support) and fixed the Refresh/Fullscreen controls (implemented handlers + fullscreen label updates).
  - *File*: `live-display.html`

### 🧾 Logging
- **`debugLogger.loading()` Compatibility Fix**: Added `DebugLogger.loading()` so modules like `RaceUI` can log loading milestones without throwing `TypeError: ...loading is not a function`.
  - *File*: `js/logger.js`

---

## Version D0L12R1.4 (Patch)

### 🖥️ Live Display
- **Recent Results Auto-Refresh Fix**: Fixed the Live Display's 8s refresh clearing "Recent Results" by properly awaiting `getRaceBracket()` and re-extracting races from the bracket during refresh.
  - *File*: `live-display.html`

### 🏁 Races
- **Live Display App Init Crash Fix**: Hardened `RaceUI` session persistence calls so pages that don't include `SessionPersistence` (e.g., Live Display) don't crash initialization (`Cannot read properties of undefined (reading 'Races')`).
  - *File*: `js/race-ui.js`

---

## Version D0L12R1.3 (Patch)

### 📅 Events
- **Event Card Details Fix**: Fixed event card details rendering too wide by scoping analytics dropdown `.event-details` flex styles so they no longer override the event cards layout.
  - *File*: `styles.css`
- **Drivers / Registrations Counts Fix**: Corrected event driver + registration counts to reflect actual event registrations (`event.participants` / `participant.eventIds`) and event-specific class entries (`eventClasses[eventId]`).
  - *File*: `utils/data-manager.js`
- **View Modal "Edit Event" Button Fix**: Fixed the modal's Edit button closing the overlay immediately after opening the edit form (corrected click handler order).
  - *File*: `events.html`

### 📊 Analytics
- **Chart Resize Fix (Analytics Dashboard)**: Wrapped Chart.js canvases in fixed-height containers to prevent runaway canvas growth during responsive resizing (e.g., `height: 26796px`) in Lane Performance and other charts.
  - *File*: `analytics.html`

### 🏁 Races
- **"Back to Events" Button Fix**: Bound RaceUI's delegated listeners during races page initialization so the "Back to Events" button (and other race-management actions) work reliably. Also prevented event-card selection from firing when clicking action buttons/links.
  - *Files*: `races.html`, `js/race-ui.js`

---

## Version D0L12R1.2 (Patch)

### 📊 Event Analytics
- **Event Analytics Overhaul**: Replaced mock charts with real, event-driven analytics and corrected several data wiring issues.
  - **Pace vs Standard**: Added an Event Pace chart (runs/hour) with a 7-minute standard baseline and improved pace calculations using start-to-start cycle times.
  - **Driver Demographics**: Added Driver Age Distribution (DOB-based, calculated at event date).
  - **Event Time Analysis**: Fixed event duration calculation, improved timestamp extraction, added time-saved vs standard metric, and prevented Chart.js canvases from being destroyed when data is missing.
  - **Event-Specific Panels**: Class breakdown, driver stats, and lane win % now derive from the selected event's bracket/results (not lifetime participant totals). Email/PDF "Send Event Stats" now uses event-specific stats.
  - *File*: `event-analytics.html`

### 👤 Driver Profiles
- **Driver Profile Stats Fix**: Fixed driver stats not populating by aligning analytics heat completion detection with the current bracket schema (`status: 'completed'` + results) and hardening lane/finish-position parsing to avoid FS/DSQ type errors.
  - **Access Model**: Driver profile page now allows `registration` users (and keeps legacy `drivers profile` permission) so registration workflows can view driver performance.
  - **UX**: Added a clear "No completed races yet" banner and chart/table placeholders instead of blank widgets when a driver has no completed race data.
  - **Lane Performance UI**: Made the Lane Statistics table compact, scroll-safe, and resilient to malformed/oversized lane data (sorted lanes + safety cap).
  - **Chart Resize Fix**: Wrapped Chart.js canvases in fixed-height containers to prevent runaway canvas growth (e.g., `height: 32760px`) during responsive resizing.
  - *Files*: `server.py`, `utils/auth.js`, `driver-profile.html`, `tests/test_stats_driver_profile.py`

---

## Version D0L12R1.1 (Patch)

### 🔧 Fixes
- **Button Interaction**: Fixed issue where clicking icons inside buttons (e.g., "Back to Events", "Mark Event Complete") would not trigger the action. Updated event delegation to correctly handle child element clicks.
  - *File*: `js/race-ui.js`
- **Console Error Suppression**: Added error handler to suppress Cloudflare Insights blocked resource errors (caused by ad blockers). Prevents console noise from external scripts.
  - *File*: `js/app.js`
- **RaceUI Syntax Errors**: Fixed 180+ instances of corrupted/missing opening quotes in debug log statements that caused `SyntaxError` and prevented RaceUI from loading. Issues included `?` and `??` characters replacing quotes, and completely missing opening quotes before message text.
  - *File*: `js/race-ui.js`
- **Additional RaceUI Syntax Fixes**: Fixed 5 more instances of missing opening quotes in debug statements (lines 4816, 6131, 6137, 6142, 6155) that caused `Unexpected token '==='` syntax errors and prevented RaceUI from loading.
  - *File*: `js/race-ui.js`
- **RaceUI Parse Error (Heat Completion Log)**: Fixed a malformed `debugLogger` call (missing quote) that caused `Uncaught SyntaxError: missing ) after argument list` and prevented `RaceUI` from being defined on the races page.
  - *Files*: `js/race-ui.js`, `races.html`
- **Debug Instrumentation Cleanup**: Removed temporary debug-mode instrumentation after verification.
  - *Files*: `races.html`, `js/race-ui.js`, `.cursorignore`

---

## Version D0L12R1.0 (Major Release - Comprehensive System Update)

**Summary**: This major release represents a significant milestone for EPC17, consolidating months of development into a stable, feature-rich platform for professional race management. It includes a complete overhaul of the user interface, robust backend improvements, enhanced analytics, and a refined pairing engine.

### 🌟 Core Features
- **Deterministic Pairing Engine**:
  - Guaranteed rematch avoidance until necessary.
  - Intelligent lane balancing and bye-run handling.
  - Tie-breaker race generation for ranking resolution.
- **Advanced Event Management**:
  - Support for multi-class events with custom rules.
  - Series management for championship tracking.
  - "Late Driver Injection" for adding participants mid-event.
- **Robust Registration System**:
  - Full CRUD for participants with tech sheet support.
  - Unified "Driver Edit Form" for consistent data management.
  - Optimized data loading for large participant lists.

### 🎨 UI/UX Overhaul
- **Modern Design System**:
  - Complete visual refresh with consistent spacing, typography, and color palette.
  - Glassmorphism effects, animated transitions, and polished component libraries.
  - Mobile-responsive layout with touch-optimized targets.
- **Enhanced Interactions**:
  - Non-blocking race result processing for faster workflows.
  - Toast notification system for user feedback (z-index fixed).
  - Improved sidebar navigation with "Access Denied" handling.

### 📊 Analytics & Reporting
- **Centralized Analytics Engine**:
  - Unified computation layer for event, series, and driver stats.
  - Ad-blocker bypass fix (API renamed to `/api/stats`).
- **Visualizations**:
  - Interactive charts for win rates, lane bias, and class distribution.
  - "Driver Profiles" with deep-dive performance metrics.
- **Data Integrity**:
  - Correct handling of completed event brackets in analytics.
  - Fix for stress test data generation structure.

### 🔧 Backend & Architecture
- **Persistence Layer**:
  - SQLite-backed session management (persists across restarts).
  - UUID-based ID system for reliable data tracking.
  - Automatic backups and migration utilities.
- **Performance**:
  - Optimized data fetching (lazy loading, batched requests).
  - "DebugLogger" system for reduced console noise.
  - Refactored event data services to prevent circular dependencies.

### 🔒 Security & Permissions
- **Role-Based Access Control (RBAC)**:
  - Granular permissions (Events, Races, Admin, etc.).
  - Secure session handling with expiry cleanup.
  - Graceful 403 error handling and redirection.

### 🐛 Key Bug Fixes
- **Race Logic**:
  - Fixed "Swap Same Heat" validation scope.
  - Resolved "Brackets already initialized" error blocking generation.
  - Corrected handling of duplicate participants in bracket generation.
- **System Stability**:
  - Fixed stack overflow in event class activation.
  - Resolved missing bracket data for synthetic events.
  - Fixed `console.debug` crash in older browsers/contexts.

---
*For historical patch notes prior to D0L12R1.0, see legacy entries in this file.*
