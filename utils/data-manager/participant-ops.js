/**
 * Participant Operations sub-module for DataManager
 * Handles all participant-related CRUD operations, data normalization,
 * migration, and optimization.
 * 
 * Target methods to migrate from data-manager.js:
 *   - getParticipants()
 *   - fetchParticipantsPage()
 *   - addParticipant()
 *   - updateParticipant()
 *   - deleteParticipant()
 *   - getParticipant()
 *   - getParticipantsArray()
 *   - getEventParticipantStats()
 *   - loadParticipantsForEvent()
 *   - normalizeParticipantData()
 *   - migrateParticipantStructure()
 *   - migrateAllParticipants()
 *   - optimizeParticipantData()
 *   - loadAllParticipantPages()
 *   - registerParticipantForEvent()
 *   - removeParticipantFromEvent()
 *   - migrateParticipantEventIds()
 * 
 * @module data-manager/participant-ops
 */

// Stub — methods remain in data-manager.js during incremental migration.
