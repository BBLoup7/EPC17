/**
 * Race Operations sub-module for DataManager
 * Handles all race and race bracket-related CRUD operations.
 * 
 * Target methods to migrate from data-manager.js:
 *   - getRaces()
 *   - getRacesArray()
 *   - addRace()
 *   - loadRaceBrackets()
 *   - saveRaceBracket()
 *   - updateRaceBracket()
 *   - deleteRaceBracket()
 *   - getRaceBracket()
 *   - getRaceBracketById()
 *   - getRaceBrackets()
 *   - cleanupDuplicateBrackets()
 * 
 * @module data-manager/race-ops
 */

// Stub — methods remain in data-manager.js during incremental migration.
