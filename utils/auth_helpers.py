"""
Auth Helpers — Session and permission management.
Target functions/data to extract from server.py:
    - SESSIONS dict (in-memory session store)
    - create_session(user_data)
    - get_session_from_request()
    - require_permission(permission) decorator
    - ensure_admin(session)
    - Duplicate require_permission fix (lines ~132 and ~234 in server.py)
"""
# Stub — functions remain in server.py during incremental migration.
