"""
Server Helpers — Shared utilities for Flask route handlers.
Target functions to extract from server.py:
    - log_debug(category, message, data)
    - generate_id(prefix)
    - emit_websocket_event(event, data)
    - is_heat_completed(heat)
    - CORS setup
"""
# Stub — functions remain in server.py during incremental migration.
